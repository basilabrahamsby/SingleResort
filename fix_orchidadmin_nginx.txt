# Optimized Nginx configuration block for Orchid Admin
location /orchidadmin {
    alias /var/www/html/orchidadmin/;
    index index.html;
    # Correct handling for SPA in subfolder with alias
    try_files $uri $uri/ /orchidadmin/index.html;
    add_header Cache-Control "no-store, no-cache, must-revalidate, max-age=0";
}

# Redirect no-slash version to slash version
# location = /orchidadmin {
#     return 301 $scheme://$http_host/orchidadmin/;
# }
